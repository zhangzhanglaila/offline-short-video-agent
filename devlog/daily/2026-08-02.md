# E5 完成 daily log (2026-08-02)

## 阶段概述
E5 ComfyUI 集成。13 个 task,每个独立 commit,subagent-driven。

## 完成事项
- ComfyUI HTTP client (services/comfyui/client.py): submit / wait_for_completion / download_outputs / test_connection
- Workflow template loader with `{{prompt}} {{width}} {{height}} {{steps}} {{seed}}` interpolation + brace-escape
- ComfyUIImageGenerator / ComfyUIVideoGenerator adapting project's AIGenerationService / AIVideoGenerationService
- 3 builtin workflow JSONs: flux_dev.json (image), wan22_t2v.json + wan22_i2v.json (video)
- MaterialFetchAgent now reads `ai.image_provider` / `ai.video_provider` from config.yaml
- Tests: config loader + template + client + image gen + video gen + integration (skip-when-unreachable)

## 设计取舍
- 原生 HTTP (方案 B) vs comfykit SDK (方案 A):选 B - 零新依赖,与现有 E2/E3 风格一致
- 默认 provider: comfyui (本地免费);云端作为可手动切换降级
- TTS 模块明确排除(E5 范围仅 image + video)
- Python 包 lazy import (PEP 562) 让 template.py 在 client.py 之前可导入

## 测试统计
- 新增: tests/test_config_loader (3) + tests/test_comfyui_template (6) + tests/test_comfyui_client (9) + tests/test_comfyui_image_generator (4) + tests/test_comfyui_video_generator (4) + tests/test_comfyui_integration (5 + 2 skipped) = 31 新测试
- 回归: full offline suite 全绿

## 已知小问题(可后续清理)
- `services/ai_image/comfyui_generator.py` 的 `_CLIENT_INJECTED` 哨兵未使用(只有 `_client_injected` 布尔在用) — minor
- MaterialFetchAgent 的 `_ai_image_enabled` / `_ai_video_enabled` 现已不被使用 — 半遗留
- `try/except Exception` 在 properties 中较宽,但匹配既有模式

## 后续可增强(非本阶段)
- ComfyUI I2V 工作流集成到 MaterialFetchAgent (现在 workflow 文件已有,generator 还未用)
- Wan 2.2 长视频生成超时单独配置
- 动态工作流(根据场景复杂度选不同 sampler 配置)
- TTS 集成(明确不在 E5 范围,留 E5.1?)

---

## 测试输出(关键)

### 完整离线回归
```
pytest tests/ -q --ignore=tests/integration
```
- 结果: 767 passed, 2 skipped (2 pre-existing flaky tests in tests/template/test_integration.py — 与 E5 无关,详见下方)

### ai_image 回归守门
```
pytest tests/test_ai_image_service.py -q
```
- 结果: 6 passed

### Integration 测试(本地无 ComfyUI,自动 skip)
```
pytest tests/test_comfyui_integration.py -v -m integration
```
- 结果: 2 skipped (ComfyUI 不可达)

## E5 价值描述

- **本地免费**: ComfyUI 开源 + 用户本地 GPU,零 API 费用
- **Provider 可切换**: config.yaml 配置 `ai.image_provider` / `ai.video_provider`,云端/本地/ComfyUI 自由切换
- **降级保底**: ComfyUI 不可达时 fallback 到现有云端路径
- **可扩展**: 3 个内置 workflow JSON,用户可接入自定义工作流

---

*最后更新: 2026-08-02*
*状态: E5 ComfyUI 集成完成 | 31 新测试 + 全绿回归 | provider 可切换*
